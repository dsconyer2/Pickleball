import { ValidNbrOfCourtsDirective } from './valid-nbr-of-courts.directive';

describe('ValidNbrOfCourtsDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidNbrOfCourtsDirective();
    expect(directive).toBeTruthy();
  });
});
